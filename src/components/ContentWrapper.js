import styled from "styled-components";

const ContentWrapper = styled.div`
  > p {
    font-weight: bold;
    margin: 0px;
    padding: 0px;
  }

  hr {
    border: 1px #0170c0 solid;
  }
`;

export default ContentWrapper;
